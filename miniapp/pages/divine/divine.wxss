.page { background-color: #f5f2ed; min-height: 100vh; }

.header {
  display: flex; align-items: center; justify-content: space-between;
  padding-top: 20rpx; padding-bottom: 20rpx;
}
.back-btn {
  width: 72rpx; height: 72rpx; border-radius: 50%;
  background: var(--card); box-shadow: 0 2rpx 16rpx rgba(0,0,0,0.04);
  display: flex; align-items: center; justify-content: center;
  font-size: 32rpx;
}
.header-title { font-size: 32rpx; font-weight: 500; }
.header-spacer { width: 72rpx; }

.hero {
  margin-top: 16rpx; padding: 40rpx; border-radius: 32rpx;
  box-shadow: 0 2rpx 16rpx rgba(0,0,0,0.04);
}
.hero-title { font-size: 40rpx; font-weight: 600; }
.hero-sub { font-size: 24rpx; color: var(--muted-foreground); margin-top: 8rpx; display: block; }

.form { padding-top: 48rpx; padding-bottom: 120rpx; }
.field { margin-bottom: 32rpx; }
.field-label {
  display: block; font-size: 24rpx; color: var(--muted-foreground);
  margin-bottom: 12rpx; font-weight: 500;
}
.field-hint { font-size: 22rpx; color: var(--muted-foreground); margin-top: 8rpx; display: block; }

.gender-row { display: flex; gap: 16rpx; }
.gender-btn {
  flex: 1; padding: 24rpx; border-radius: 24rpx;
  border: 2rpx solid var(--border); background: var(--card);
  font-size: 28rpx; text-align: center;
}
.gender-btn.active { border-color: var(--primary); background: rgba(139,92,246,0.1); color: var(--primary); }

.form-input {
  width: 100%; padding: 24rpx 32rpx;
  border: 2rpx solid var(--border); border-radius: 24rpx;
  background: var(--card); font-size: 28rpx; box-sizing: border-box;
}

.date-row-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 16rpx; }
.picker-display {
  padding: 24rpx 16rpx; border: 2rpx solid var(--border);
  border-radius: 24rpx; background: var(--card);
  font-size: 28rpx; text-align: center;
}

.time-row { display: flex; gap: 16rpx; align-items: center; }
.estimate-btn {
  padding: 24rpx 32rpx; border-radius: 24rpx;
  border: 2rpx dashed rgba(139,92,246,0.5);
  font-size: 24rpx; color: var(--primary);
  background: transparent; flex-shrink: 0;
}

.save-role {
  padding: 32rpx; border-radius: 24rpx;
  border: 2rpx solid var(--border); background: var(--card);
  margin-bottom: 32rpx;
}
.save-role.active { border-color: var(--primary); background: rgba(139,92,246,0.05); }
.save-role-label { display: flex; align-items: flex-start; gap: 16rpx; }
.save-role-title { font-size: 28rpx; font-weight: 500; }
.save-role-count {
  padding: 4rpx 16rpx; border-radius: 9999rpx;
  background: var(--muted); font-size: 20rpx; color: var(--muted-foreground);
}
.save-role-count.full { background: rgba(217,78,60,0.15); color: var(--cinnabar); }
.save-role-hint { font-size: 22rpx; color: var(--muted-foreground); display: block; }

.role-extra { margin-top: 24rpx; }
.mb-1 { margin-bottom: 8rpx; }
.mb-2 { margin-bottom: 16rpx; }

.avatar-pick { display: flex; flex-wrap: wrap; gap: 12rpx; }
.avatar-item {
  width: 72rpx; height: 72rpx; border-radius: 16rpx;
  background: var(--muted); display: flex;
  align-items: center; justify-content: center; font-size: 36rpx;
}
.avatar-item.active { background: rgba(139,92,246,0.15); border: 4rpx solid var(--primary); }

.btn-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16rpx; padding-top: 16rpx; }
.gen-btn {
  padding: 32rpx 16rpx; border-radius: 24rpx;
  font-size: 28rpx; font-weight: 500;
  display: flex; align-items: center; justify-content: center;
  flex-direction: column; border: none;
}
.gen-btn.primary { background: var(--primary); color: var(--primary-foreground); box-shadow: 0 8rpx 32rpx rgba(0,0,0,0.08); }
.gen-btn.outline { background: var(--card); color: var(--primary); border: 2rpx solid var(--primary); }
.gen-btn::after { border: none; }
.gen-btn:active { transform: scale(0.98); }

.privacy-tip {
  margin-top: 32rpx; padding: 32rpx; border-radius: 24rpx;
  background: rgba(243,244,246,0.6);
  font-size: 24rpx; color: var(--muted-foreground);
}
